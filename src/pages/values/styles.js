import { StyleSheet } from "react-native";

const Style = StyleSheet.create({
  img: {
    width: 60,
    height: 60,
  },

  container: {
    flexDirection: "row",
    alignItems: "center",
    paddingBottom: 30,
  },

  textStyle: {
    paddingLeft: 20,
    color: "white",
    fontSize: 20,
    lineHeight: 24,
  },
});

export default Style;
