import { StyleSheet } from "react-native";

const Style = StyleSheet.create({
  img: {
    width: 60,
    height: 60,
  },

  container: {
    flexDirection: "row",
    alignItems: "center",
    paddingBottom: 30,
  },

  textStyle: {
    paddingLeft: 20,
    color: "#fff",
    fontSize: 20,
    lineHeight: 24,
  },

  byPerson: {
    fontWeight: "bold",
    fontSize: 22,
    color: "#fff",
  },
});

export default Style;
